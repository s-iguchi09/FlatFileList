<Application
    x:Class="FlatFileList.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:FlatFileList"
    StartupUri="MainWindow.xaml">
    <Application.Resources>

        <Style x:Key="TextBlockBase" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="CheckBoxBase" TargetType="{x:Type CheckBox}" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style
            x:Key="{x:Type TextBlock}"
            BasedOn="{StaticResource TextBlockBase}"
            TargetType="{x:Type TextBlock}" />
        <Style
            x:Key="{x:Type CheckBox}"
            BasedOn="{StaticResource CheckBoxBase}"
            TargetType="{x:Type CheckBox}" />

    </Application.Resources>
</Application>
